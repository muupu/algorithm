#include <vector>  
#include <string>  
#include <iostream> 

namespace algorithm {

	template <typename T>
	void longest_common_subsequence(const T& s1, const T& s2, T& s3)  
	{


	}  

	int main()
	{
		return 0;
	}
}